# Voice handoff — jesse-pujji (15 verified quotes)

From the epistemic-atoms session (frosty-gauss-edad49) to the jesse integrator.

Every `original_quote` here is provenance-gated: confirmed VERBATIM in jesse's local
transcript + raw corpus by `scripts/verify-enrichment.py`. ADDITIVE to the 16 quotes
already applied live on main (these are different atoms — the live ones are excluded by
`fetch_top_atoms`'s null-quote filter).

Apply as part of your single integration motion, then ONE export + ONE re-panel:

    python3 scripts/enrich-voice.py --brain jesse-pujji --apply \
        brains/jesse-pujji/research/voice-verified-handoff-2026-07-05.json

Expected effect: ~+0.02 voice (16 quotes ≈ +0.02 measured), taking voice 0.581 → ~0.60 gate.
